# Simple gnuradio oot block for mqtt #

# Build #

    mkdir build
    cd build
    cmake ../
    make
    sudo make install

# Run #

    pip install -r requirements.txt

Make sure that installed requirements are part of gnuradio's PYTHONPATH
